# zizmor configuration - GitHub Actions security auditor
# https://docs.zizmor.sh/

[rules.self-hosted-runner]
# We don't use self-hosted runners
ignore = ["*"]

[rules.undocumented-permissions]
# Our permissions are explicit but we don't add inline comments on each one
ignore = ["*"]
