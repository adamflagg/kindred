# openapi-typescript@7.x declares typescript@^5.x as a peer dep, but this
# project uses typescript@^6.x. Until upstream adds TS 6 support
# (drwpow/openapi-typescript#1840), we accept the loose peer resolution.
# Required for both `npm install` (local) and `npm ci` (CI).
legacy-peer-deps=true
