const GITHUB_REPO_URL = 'https://github.com/adamflagg/kindred'

/** Parse a version string from the build pipeline. Stub — not yet implemented. */
export function parseVersion(_version: string): { display: string; url: string } | null {
  return null
}

export { GITHUB_REPO_URL }
