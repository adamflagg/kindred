/**
 * @deprecated File scheduled for deletion in this PR — re-exporting from
 * `satisfactionLookup.ts` while consumers are migrated. Do not import from
 * this file in new code.
 */
export {
  evaluateRequest as computeRequestSatisfaction,
  resolveBadgeBucket,
  type BunkmateInfo,
  type SatisfactionStatus,
  type SatisfactionResult,
  type EvaluateRequestInputs as ComputeRequestSatisfactionInputs,
} from './satisfactionLookup'
