/**
 * Canonical GitHub URL for this repo. Used to build commit links from
 * `solver_runs.git_sha` etc. Single source of truth — if the repo ever moves,
 * change it here and everywhere that imports from this module updates.
 */
export const REPO_URL = 'https://github.com/adamflagg/kindred'
